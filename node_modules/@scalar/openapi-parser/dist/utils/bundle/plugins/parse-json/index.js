import { isJson } from "../../../../utils/is-json.js";
function parseJson() {
  return {
    validate: isJson,
    exec: async (value) => {
      try {
        return {
          ok: true,
          data: JSON.parse(value)
        };
      } catch {
        return {
          ok: false
        };
      }
    }
  };
}
export {
  parseJson
};
//# sourceMappingURL=index.js.map
