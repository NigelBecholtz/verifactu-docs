import { z } from "zod";
import { transformProtectedRoutes } from "../../lib/core/ZudokuContext.js";
export const ProtectedRoutesInputSchema = z
    .union([
    z.string().array(),
    z.record(z.string(), z.custom((val) => typeof val === "function")),
])
    .optional();
export const ProtectedRoutesSchema = ProtectedRoutesInputSchema.transform(transformProtectedRoutes);
//# sourceMappingURL=ProtectedRoutesSchema.js.map